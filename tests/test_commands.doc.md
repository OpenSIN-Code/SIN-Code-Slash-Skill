# Purpose: Tests for built-in commands.
# Docs: tests/test_commands.doc.md
# test_commands.py

## What this file does
Comprehensive tests for built-in command definitions verifying correct metadata and help text.

## Test coverage
- All 10 built-in commands exist
- Each command has correct description, action, and type
- Lookup by name (existing, missing)
- List all commands
- Help text generation
- All commands have required fields (description, action, type, help)
